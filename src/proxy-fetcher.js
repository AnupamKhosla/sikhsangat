// DISABLED: We now use the 3*3 Tiered Stealth Method (Tor, Firefox/SOCKS5, CORS Pool).
// Proxy fetching from public lists is no longer used to avoid local IP leaks and instability.
console.log("[ SYSTEM ] Proxy Fetcher is disabled. Using 3*3 Strategy defined in GEMINI.md.");
process.exit(0);
